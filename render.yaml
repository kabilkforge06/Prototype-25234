services:
  - type: web
    name: cvd-detect
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python App.py
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PORT
        value: 10000
      - key: SECRET_KEY
        generateValue: true